version: 2
jobs:
  build-and-test:
    machine: true
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            set -x
            docker-compose -f docker-compose-test.yml run tests

workflows:
  version: 2
  build_and_test:
    jobs:
      - build-and-test